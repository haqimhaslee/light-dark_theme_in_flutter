import 'package:flutter/material.dart';

//Color setting
const darkBackground = Color.fromARGB(255, 36, 36, 36);
const lightBackground = Color.fromARGB(255, 255, 255, 255);

class MyThemes {
  static final darkTheme = ThemeData(scaffoldBackgroundColor: darkBackground);

  static final lightTheme = ThemeData(scaffoldBackgroundColor: lightBackground);
}
